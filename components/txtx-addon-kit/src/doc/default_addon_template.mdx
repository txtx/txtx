export const metadata = {
    title: "{{ addon_name }}",
    description: "Add-on for {{ addon_name }}",
};

# {{ addon_name }} {{double_open}} id: "{{ addon_namespace }}-constructs" {{double_close}}

{{ addon_description }}

---

{{# functions }}

## {{ name }} {{double_open}} tag: 'function' {{double_close}}

<Row>
  <Col>

{{{ documentation }}}

### Inputs Documentation

<Properties>

{{# inputs }}

  <Property name="{{ name }}">
    {{{ documentation }}}
  </Property>

{{/ inputs }}

</Properties>

</Col>
</Row>

---

{{/ functions }}

{{# actions }}

## {{ matcher }} {{double_open}} tag: 'action' {{double_close}}

### {{ name }}

{{{ documentation }}}

### Inputs Documentation

<Properties>

{{# inputs }}

  <Property name="{{ name }}" type="required">
    {{{ documentation }}}
  </Property>

{{/ inputs }}

</Properties>

<CodeGroup title="Example using {{ matcher }}">


```hcl {{double_open}} title: 'main.tx' {{double_close}}
{{{ example }}}
```


```json {{double_open}} title: 'main.json' {{double_close}}
// Coming soon
```

```yaml {{double_open}} title: 'main.yaml' {{double_close}}
# Coming soon
```

</CodeGroup>

### Outputs Documentation

<Note>

When the `{{ matcher }}` action is successfully executed, the following outputs are attached to the action

</Note>

<Properties>

{{# outputs }}

  <Property name="{{ name }}">
    {{{ documentation }}}
  </Property>

{{/ outputs }}

</Properties>

---

{{/ actions }}

{{# prompts }}

## {{ matcher }} {{double_open}} tag: 'prompts' {{double_close}}

### {{ name }}

{{{ documentation }}}

### Inputs Documentation

<Properties>

{{# inputs }}

  <Property name="{{ name }}" type="required">
    {{{ documentation }}}
  </Property>

{{/ inputs }}

</Properties>

<CodeGroup title="Example using {{ matcher }}">


```hcl {{double_open}}title: 'main.tx' {{double_close}}
{{{ example }}}
```


```json {{double_open}} title: 'main.json' {{double_close}}
// Coming soon
```

```yaml {{double_open}} title: 'main.yaml' {{double_close}}
# Coming soon
```

</CodeGroup>

### Outputs Documentation

<Note>

When the `{{ matcher }}` prompt is successfully executed, the following outputs are attached to the prompt

</Note>

<Properties>

{{# outputs }}

  <Property name="{{ name }}">
    {{{ documentation }}}
  </Property>

{{/ outputs }}

</Properties>

---

{{/ prompts }}
